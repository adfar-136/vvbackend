//MERN

// 1. React 
 
   //tailwind & Material UI (mandatory responsive) component + responsiveness

   // props , state => useState and Redux , useReducer ,state batching and scheduling;
   // hooks => useEffect,useContext,useCallback,useMemo,useRef,useReducer
   // React Router (client side routing) - react-router-dom => browserrouter routes (nestingrouting outlets)  route LINK NavLInk params useParams useNavigate
   // error boundaries, context API, memoization, lazy loading, suspense

//2 Express 
  // node fs, url, path,os ,events ,streams,http
  // express => express app, router, middleware, request, response, next
  //,route,middlewares (authorization (passport js)),controllers,services,models
 // router.get("/",verifyUser,getUser)
 // router.get("/register",createUser)
 // function getUser(){ interact with the database User.findByID(id) }
//Mongodb